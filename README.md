# These codes are written in Zemax programming language (i.e. ZPL-MACROS) and ZOS-API with MATLAB.
# ZPL and ZOS-API allows easy sharing as the file can simply be a .txt file, which the receiver can convert to the lens file (.zmx/.zos) using Zemax OpticStudio
# Allows unlimited commenting, unlike lens file (my reason for using ZPL MACROS ZOS-API)
# Please note these files have been tested with OpticStudio 2024 so get in touch with me if you have trouble running them
